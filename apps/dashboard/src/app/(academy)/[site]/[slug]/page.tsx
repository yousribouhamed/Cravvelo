import type { FC } from "react";

export const fetchCache = "force-no-store";

interface pageAbdullahProps {}

const page: FC = ({}) => {
  return (
    <div>
      <h1>this is the sug page in the multy tanasity</h1>
    </div>
  );
};

export default page;
