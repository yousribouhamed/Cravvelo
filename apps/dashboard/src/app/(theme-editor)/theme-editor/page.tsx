import type { FC } from "react";
import ThemeEditorHeader from "../_components/theme-editor-header";
import ThemeEditorBoard from "../_components/theme-editor-board";
interface pageAbdullahProps {}

const page: FC = ({}) => {
  return <ThemeEditorBoard />;
};

export default page;
